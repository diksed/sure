development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV["REDIS_CABLE_URL"] || ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: sure_production
